// cargo new creates a new package 
// or crate -> 1.Binary crate 2.Library crate(can used by other programs)
// module -> way to organize teh programs 
// and also apply the privacy rules 
// if the rust project has main.rs(that means it has one binary crate)
// lib.rs -> one library crate 
// /bin(going to hold all of the binary crates )
